# Folder Ảnh Chứng Chỉ

✨ **BẠN KHÔNG CẦN ĐỔI TÊN FILE!**

Chỉ cần đặt ảnh chứng chỉ vào folder này với **BẤT KỲ TÊN NÀO** - hệ thống sẽ tự động tìm và hiển thị tất cả ảnh.

## 📸 Cách chụp màn hình PDF thành PNG:

### Cách 1: Sử dụng Windows Snipping Tool
1. Mở file PDF trong trình đọc PDF (Adobe Reader, Edge, Chrome)
2. Nhấn `Windows + Shift + S` để mở Snipping Tool
3. Chọn vùng chứng chỉ muốn chụp
4. Lưu ảnh với tên bất kỳ (ví dụ: `JavaScriptEssentials.png`)
5. Copy ảnh vào folder này

### Cách 2: Sử dụng Windows Print Screen
1. Mở PDF ở chế độ toàn màn hình
2. Nhấn `Print Screen` (PrtScn)
3. Mở Paint hoặc Photoshop
4. Dán ảnh (`Ctrl + V`)
5. Cắt và chỉnh sửa ảnh
6. Lưu dưới dạng PNG
7. Copy ảnh vào folder này

### Cách 3: Export PDF thành ảnh (Adobe Reader)
1. Mở PDF trong Adobe Reader
2. File → Export To → Image → PNG
3. Chọn chất lượng và lưu
4. Copy ảnh vào folder này

**Ví dụ tên file:**
- `cert1.png`, `cert2.png`, `cert3.png`, `cert4.png`
- `JavaScriptEssentials.png`, `basic.png`
- `I2CSUpdate.png`, `chung-chi-1.png`
- Hoặc bất kỳ tên nào khác!

**Định dạng được hỗ trợ:** `.jpg`, `.jpeg`, `.png` (PNG được khuyến nghị)

**Kích thước khuyến nghị:** Tỷ lệ 4:3 hoặc 3:4, độ phân giải tối thiểu 800x600px

**Sau khi đặt ảnh vào, refresh trang resume và chứng chỉ sẽ tự động hiển thị!**

